
<% other_user = current_user.id = chat.sender_id ? chat.recipient : chat.sender %>
<%= link_to chat do %>
  <div class="row chat">
    <% if chat.messages.any? %>
      <div class="col-md-4">
        <%= other_user.email %>

        <%= chat.messages.last.message_time %>
      </div>
      <div class="col-md-8">
        <%= chat.messages.last.body %>
      </div>
    <% end %>
  </div>
<% end %>
